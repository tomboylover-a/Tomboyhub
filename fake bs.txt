local repo = "https://raw.githubusercontent.com/deividcomsono/Obsidian/main/"

local successLib, Library = pcall(function()
    return loadstring(game:HttpGet(repo .. "Library.lua"))()
end)

local successTM, ThemeManager = pcall(function()
    return loadstring(game:HttpGet(repo .. "addons/ThemeManager.lua"))()
end)

local successSM, SaveManager = pcall(function()
    return loadstring(game:HttpGet(repo .. "addons/SaveManager.lua"))()
end)

if not successLib or not Library then
    warn("Tomboy Hub: Failed to load Obsidian Library from GitHub.")
    return
end

local Players = game:GetService("Players")
local ReplicatedStorage = game:GetService("ReplicatedStorage")
local RunService = game:GetService("RunService")
local LocalPlayer = Players.LocalPlayer
local Workspace = game:GetService("Workspace")
local Camera = Workspace.CurrentCamera

Library.ShowToggleFrameInKeybinds = true

L = L or {}
L.tomboyHitterEnabled = false
L.zombieHitboxEnabled = false
L.zombieHitboxVisualsEnabled = true
L.zombieHitboxSize = 10
L.zombieHitboxTransparencyPercent = 50
L.zombieHitboxAddedParts = {}

L.removeParticlesEnabled = false
L.removeHorsesEnabled = false

L.attackSpeedEnabled = false
L.attackSpeedMultiplier = 1

L.aimbotEnabled = false
L.aimbotFOV = 300
L.aimbotTargetPart = "Head"
L.aimbotMaxRange = 500
L.fovCircleVisible = false
L.fovCircleFilled = false

L.customWallCheckEnabled = true
local v3new = Vector3.new
local WALL_CHECK_SAMPLE_AXIS_SCALE = 0.4
local WALL_CHECK_MIN_SAMPLE_OFFSET = 0.5
local WALL_CHECK_MAX_SAMPLE_OFFSET = 3.0

L.targetShambler = true
L.targetRunner = true
L.targetBomber = true
L.targetCuirassier = true
L.targetZapper = true
L.targetIgniter = true

getgenv().espShamblerToggled = false
getgenv().espRToggled = false
getgenv().espBToggled = false
getgenv().espCuToggled = false
getgenv().espZToggled = false
getgenv().espIgniterToggled = false

local hookInstalled = false
local originalMeleeHitCheck = nil
local lastZombieHitTime = {}
local ZOMBIE_HIT_COOLDOWN = 0.1
local currentLockedTarget = nil

local fovDrawingCircle = Drawing.new("Circle")
fovDrawingCircle.Visible = false
fovDrawingCircle.Transparency = 0.7
fovDrawingCircle.Thickness = 2
fovDrawingCircle.Color = Color3.fromRGB(200, 50, 255)
fovDrawingCircle.NumSides = 64
fovDrawingCircle.Filled = false

local lockBox = Drawing.new("Square")
lockBox.Visible = false
lockBox.Thickness = 2
lockBox.Color = Color3.fromRGB(0, 255, 100)
lockBox.Filled = false
lockBox.Transparency = 0.9

_G.MusicianAutoPlay = false

local function HookBeatTracker()
    local modules = ReplicatedStorage:FindFirstChild("Modules")
    if not modules then return end
    local gameplay = modules:FindFirstChild("Gameplay")
    if not gameplay then return end
    local beatTrackerModule = gameplay:FindFirstChild("BeatTracker")
    if not beatTrackerModule then return end
    
    local success, BeatTracker = pcall(require, beatTrackerModule)
    if not success or not BeatTracker then return end
    
    if _G.BeatTrackerHooked then return end
    _G.BeatTrackerHooked = true

    local oldUpdate = BeatTracker.update
    BeatTracker.update = function(self, dt)
        local v1 = tick()
        local beatHappened = false
        if self.nextBeat < v1 then
            beatHappened = true
        end

        oldUpdate(self, dt)

        if beatHappened and _G.MusicianAutoPlay then
            task.defer(function()
                if _G.MusicianAutoPlay and self.songSource and self.songSource.IsPlaying then
                    self:hit()
                end
            end)
        end
    end
end

pcall(HookBeatTracker)

local function applyBuffToTool(tool)
    if not tool:IsA("Tool") then return end
    local buff = tool:FindFirstChild("SwingSpeedBuff")
    if L.attackSpeedEnabled then
        if not buff then
            buff = Instance.new("NumberValue")
            buff.Name = "SwingSpeedBuff"
            buff.Parent = tool
        end
        buff.Value = L.attackSpeedMultiplier
    else
        if buff then buff:Destroy() end
    end
end

function L.updateAllTools()
    local char = LocalPlayer.Character
    local backpack = LocalPlayer:FindFirstChild("Backpack")
    if char then
        for _, tool in char:GetChildren() do applyBuffToTool(tool) end
    end
    if backpack then
        for _, tool in backpack:GetChildren() do applyBuffToTool(tool) end
    end
end

local function setupToolListeners(char)
    char.ChildAdded:Connect(applyBuffToTool)
    local backpack = LocalPlayer:WaitForChild("Backpack", 5)
    if backpack then backpack.ChildAdded:Connect(applyBuffToTool) end
end

if LocalPlayer.Character then setupToolListeners(LocalPlayer.Character) end

local function getZombieClassification(child)
    local name = child.Name:lower()
    local isBomber = child:FindFirstChild("Barrel") ~= nil or name:find("bomber") or name:find("barrel")
    local isIgniter = child:FindFirstChild("Lantern") ~= nil or child:FindFirstChild("Torch") ~= nil or name:find("igniter") or name:find("lantern")
    local isCuirassier = child:FindFirstChild("Sword") ~= nil or name:find("cuirassier") or name:find("sword")
    local isZapper = child:FindFirstChild("Axe") ~= nil or name:find("zapper") or name:find("axe")
    
    local zombieRef = nil
    local origin = child:FindFirstChild("Orig")
    if origin and origin.Value then
        zombieRef = origin.Value
    end

    local ws = 16
    if zombieRef and zombieRef:FindFirstChild("Zombie") then
        ws = zombieRef.Zombie.WalkSpeed or 16
    elseif child:FindFirstChild("Humanoid") then
        ws = child.Humanoid.WalkSpeed or 16
    end

    local isRunner = (ws >= 20) or name:find("runner") or name:find("fast")
    local isShambler = not (isBomber or isRunner or isIgniter or isCuirassier or isZapper)

    return {
        shambler = isShambler,
        runner = isRunner,
        bomber = isBomber,
        cuirassier = isCuirassier,
        zapper = isZapper,
        igniter = isIgniter
    }
end

local function isZombieDead(zombieModel)
    if not zombieModel or not zombieModel.Parent then return true end
    local humanoid = zombieModel:FindFirstChildOfClass("Humanoid")
    local orig = zombieModel:FindFirstChild("Orig")
    if not humanoid and orig and orig.Value then
        humanoid = orig.Value:FindFirstChildOfClass("Humanoid")
    end
    if humanoid and humanoid.Health <= 0 then
        return true
    end
    return false
end

local function getZombieModel(part)
    if not part then return nil end
    local model = part.Parent
    for i = 1, 5 do
        if not model then break end
        if model:IsA("Model") and (model.Name == "m_Zombie" or model:FindFirstChild("Orig")) then
            return model
        end
        model = model.Parent
    end
    return nil
end

local function getZombieHead(zombieModel)
    if not zombieModel then return nil end
    for _, part in zombieModel:GetChildren() do
        if part.Name == "Head" and (part:IsA("Part") or part:IsA("MeshPart")) then
            return part
        end
    end
    return nil
end

local function unifiedMeleeHitCheck(self, origin, direction, raycastParams, hitEntities, isCharge)
    local rayResult = workspace:Raycast(origin, direction, raycastParams)
    if rayResult and L.tomboyHitterEnabled then
        local hitPart = rayResult.Instance
        local zombieModel = getZombieModel(hitPart)
        if zombieModel then
            if isZombieDead(zombieModel) then
                return 0 
            end

            local now = tick()
            if lastZombieHitTime[zombieModel] and now - lastZombieHitTime[zombieModel] < ZOMBIE_HIT_COOLDOWN then
                return 0
            end
            local orig = zombieModel:FindFirstChild("Orig")
            if orig then
                local head = getZombieHead(zombieModel)
                if head then
                    local zombieRef = orig.Value
                    local hitPos = head.Position
                    local partName = "Head"
                    local char = LocalPlayer.Character
                    local headPart = char and char:FindFirstChild("Head")
                    local Direction = headPart and (hitPos - headPart.Position).Unit or Vector3.new(0, 1, 0)
                    if isCharge then
                        self.remoteEvent:FireServer("ThrustCharge", zombieRef, hitPos, rayResult.Normal)
                    else
                        self.remoteEvent:FireServer("HitZombieM", zombieRef, hitPos, true, hitPos, partName, Direction)
                    end
                    lastZombieHitTime[zombieModel] = now
                    return 1
                end
            end
        end
    end
    if originalMeleeHitCheck then
        return originalMeleeHitCheck(self, origin, direction, raycastParams, hitEntities, isCharge)
    end
    return 0
end

local function installHook()
    if hookInstalled then return end
    local modules = ReplicatedStorage:FindFirstChild("Modules")
    if not modules then return end
    local weapons = modules:FindFirstChild("Weapons")
    if not weapons then return end
    local success, MeleeBase = pcall(require, weapons:FindFirstChild("MeleeBase"))
    if not success or not MeleeBase then return end
    if type(hookfunction) == "function" then
        if not originalMeleeHitCheck then
            originalMeleeHitCheck = hookfunction(MeleeBase.MeleeHitCheck, unifiedMeleeHitCheck)
        else
            hookfunction(MeleeBase.MeleeHitCheck, unifiedMeleeHitCheck)
        end
    else
        if not originalMeleeHitCheck then
            originalMeleeHitCheck = MeleeBase.MeleeHitCheck
        end
        MeleeBase.MeleeHitCheck = unifiedMeleeHitCheck
    end
    hookInstalled = true
end

LocalPlayer.CharacterAdded:Connect(function()
    task.wait(1)
    installHook()
end)

if LocalPlayer.Character then
    task.wait(0.5)
    installHook()
end

local function isValidZombieType(child)
    if isZombieDead(child) then return false end
    local types = getZombieClassification(child)
    if types.shambler and L.targetShambler then return true end
    if types.runner and L.targetRunner then return true end
    if types.bomber and L.targetBomber then return true end
    if types.cuirassier and L.targetCuirassier then return true end
    if types.zapper and L.targetZapper then return true end
    if types.igniter and L.targetIgniter then return true end
    return false
end

local function getWallCheckSamplePositions(part, aimPosOverride)
    if not part or not part.Parent then return {} end
    local basePos = aimPosOverride or part.Position
    local cf = part.CFrame
    local size = part.Size or v3new(1,1,1)
    local function axisOffset(comp)
        local scaled = comp * WALL_CHECK_SAMPLE_AXIS_SCALE
        if scaled < WALL_CHECK_MIN_SAMPLE_OFFSET then scaled = WALL_CHECK_MIN_SAMPLE_OFFSET end
        if scaled > WALL_CHECK_MAX_SAMPLE_OFFSET then scaled = WALL_CHECK_MAX_SAMPLE_OFFSET end
        return scaled
    end
    local xOff = axisOffset(size.X)
    local yOff = axisOffset(size.Y)
    local zOff = axisOffset(size.Z)
    local offsets = {
        v3new(0,0,0),
        v3new(0, yOff, 0),
        v3new(0, -yOff, 0),
        v3new(xOff, 0, 0),
        v3new(-xOff, 0, 0),
        v3new(0, 0, zOff),
        v3new(0, 0, -zOff)
    }
    local samples = {}
    for _, offset in ipairs(offsets) do
        samples[#samples+1] = basePos + cf:VectorToWorldSpace(offset)
    end
    return samples
end

local function isVisibleThroughWalls(targetPart, zombieModel)
    if not L.customWallCheckEnabled then return true end
    local samples = getWallCheckSamplePositions(targetPart)
    local origin = Camera.CFrame.Position
    
    local baseParams = RaycastParams.new()
    baseParams.FilterType = Enum.RaycastFilterType.Exclude
    baseParams.IgnoreWater = true
    
    local globalIgnore = {}
    if LocalPlayer.Character then table.insert(globalIgnore, LocalPlayer.Character) end
    for _, player in ipairs(Players:GetPlayers()) do
        if player.Character then table.insert(globalIgnore, player.Character) end
    end
    
    local zombiesFolder = Workspace:FindFirstChild("Zombies")
    if zombiesFolder then table.insert(globalIgnore, zombiesFolder) end
    
    local cameraFolder = Workspace:FindFirstChild("Camera")
    if cameraFolder then table.insert(globalIgnore, cameraFolder) end
    
    if zombieModel then table.insert(globalIgnore, zombieModel) end

    local function isPenetrable(part)
        if not part then return false end
        local name = part.Name:lower()
        if part.Transparency >= 0.95 or name:find("invisible") or name:find("barrier") or name:find("glass") or name:find("fence") or name:find("grate") then
            return true
        end
        return false
    end

    for _, samplePos in ipairs(samples) do
        local currentOrigin = origin
        local currentDir = samplePos - origin
        local hitTarget = false
        
        local currentIgnore = {}
        for _, v in ipairs(globalIgnore) do table.insert(currentIgnore, v) end
        
        local maxPenetrations = 5
        for i = 1, maxPenetrations do
            baseParams.FilterDescendantsInstances = currentIgnore
            local result = Workspace:Raycast(currentOrigin, currentDir, baseParams)
            
            if not result then
                hitTarget = true
                break
            elseif zombieModel and result.Instance:IsDescendantOf(zombieModel) then
                hitTarget = true
                break
            elseif isPenetrable(result.Instance) then
                table.insert(currentIgnore, result.Instance)
                local hitPos = result.Position
                currentDir = samplePos - hitPos
                currentOrigin = hitPos + (currentDir.Unit * 0.1)
                if currentDir.Magnitude <= 0 then
                    hitTarget = true
                    break
                end
            else
                break
            end
        end
        
        if hitTarget then
            return true
        end
    end
    
    return false
end

local function getClosestFilteredTarget()
    if currentLockedTarget and currentLockedTarget.Parent then
        local zombieModel = currentLockedTarget.Parent
        if zombieModel and zombieModel.Parent and isValidZombieType(zombieModel) then
            local worldDist = (currentLockedTarget.Position - Camera.CFrame.Position).Magnitude
            if worldDist <= L.aimbotMaxRange and isVisibleThroughWalls(currentLockedTarget, zombieModel) then
                local screenPos, onScreen = Camera:WorldToViewportPoint(currentLockedTarget.Position)
                if onScreen then
                    local screenCenter = Vector2.new(Camera.ViewportSize.X / 2, Camera.ViewportSize.Y / 2)
                    local distance = (Vector2.new(screenPos.X, screenPos.Y) - screenCenter).Magnitude
                    if distance <= (L.aimbotFOV * 1.5) then
                        return currentLockedTarget
                    end
                end
            end
        end
    end

    local closestTarget = nil
    local shortestDistance = math.huge
    local screenCenter = Vector2.new(Camera.ViewportSize.X / 2, Camera.ViewportSize.Y / 2)

    pcall(function()
        local function scanFolder(folder)
            if not folder then return end
            for _, child in pairs(folder:GetChildren()) do
                if child:IsA("Model") and (child.Name == "m_Zombie" or child:FindFirstChild("Orig") or child:FindFirstChild("Humanoid")) then
                    local targetPartName = (L.aimbotTargetPart == "Body") and "HumanoidRootPart" or "Head"
                    local targetPart = child:FindFirstChild(targetPartName) or child:FindFirstChild("Head") or child:FindFirstChild("HumanoidRootPart")
                    
                    if targetPart and isValidZombieType(child) then
                        local worldDist = (targetPart.Position - Camera.CFrame.Position).Magnitude
                        if worldDist <= L.aimbotMaxRange and isVisibleThroughWalls(targetPart, child) then
                            local screenPos, onScreen = Camera:WorldToViewportPoint(targetPart.Position)
                            if onScreen then
                                local screenVector = Vector2.new(screenPos.X, screenPos.Y)
                                local distance = (screenVector - screenCenter).Magnitude
                                if distance < shortestDistance and distance <= L.aimbotFOV then
                                    shortestDistance = distance
                                    closestTarget = targetPart
                                end
                            end
                        end
                    end
                end
            end
        end
        scanFolder(Workspace.Camera)
        scanFolder(Workspace:FindFirstChild("Zombies"))
    end)
    
    currentLockedTarget = closestTarget
    return closestTarget
end

RunService.RenderStepped:Connect(function()
    local screenCenter = Vector2.new(Camera.ViewportSize.X / 2, Camera.ViewportSize.Y / 2)
    
    fovDrawingCircle.Position = screenCenter
    fovDrawingCircle.Radius = L.aimbotFOV
    fovDrawingCircle.Visible = L.fovCircleVisible
    fovDrawingCircle.Filled = L.fovCircleFilled

    if not L.aimbotEnabled then
        lockBox.Visible = false
        currentLockedTarget = nil
        return
    end
    
    local targetPart = getClosestFilteredTarget()
    if targetPart then
        Camera.CFrame = CFrame.new(Camera.CFrame.Position, targetPart.Position)
        local model = targetPart.Parent
        local _, size = model:GetBoundingBox()
        local headPos = targetPart.Position
        local topScreen, topOnScreen = Camera:WorldToViewportPoint(headPos + Vector3.new(0, size.Y / 2, 0))
        local bottomScreen, bottomOnScreen = Camera:WorldToViewportPoint(headPos - Vector3.new(0, size.Y / 2, 0))
        
        if topOnScreen and bottomOnScreen then
            local height = math.abs(bottomScreen.Y - topScreen.Y)
            local width = height * 0.6
            lockBox.Size = Vector2.new(width, height)
            lockBox.Position = Vector2.new(topScreen.X - width / 2, topScreen.Y)
            lockBox.Visible = true
        else
            lockBox.Visible = false
        end
    else
        lockBox.Visible = false
        currentLockedTarget = nil
    end
end)
local function getCurrentTransparency()
    if not L.zombieHitboxVisualsEnabled then return 1 end
    return L.zombieHitboxTransparencyPercent / 100
end

local function addHitboxesToZombie(zombie)
    if not L.zombieHitboxEnabled or isZombieDead(zombie) then return end
    if L.zombieHitboxAddedParts[zombie] then return end
    local hrp = zombie:FindFirstChild("HumanoidRootPart")
    local head = zombie:FindFirstChild("Head")
    if not hrp or not head then return end

    local currentTrans = getCurrentTransparency()

    local outer = Instance.new("Part")
    outer.Name = "ZombieHitbox_Outer"
    outer.Size = Vector3.new(L.zombieHitboxSize, L.zombieHitboxSize, L.zombieHitboxSize)
    outer.Transparency = currentTrans
    outer.Color = Color3.fromRGB(160, 32, 240)
    outer.Material = Enum.Material.Neon
    outer.CanCollide = false
    outer.CanTouch = true
    outer.Massless = true
    outer.Anchored = false
    outer.CFrame = hrp.CFrame
    outer.Parent = zombie

    local weldOuter = Instance.new("WeldConstraint")
    weldOuter.Part0 = hrp
    weldOuter.Part1 = outer
    weldOuter.Parent = outer

    local headBox = Instance.new("Part")
    headBox.Name = "ZombieHitbox_Head"
    headBox.Size = Vector3.new(L.zombieHitboxSize/2, L.zombieHitboxSize/2, L.zombieHitboxSize/2)
    headBox.Transparency = currentTrans
    headBox.Color = Color3.fromRGB(200, 50, 255)
    headBox.Material = Enum.Material.Neon
    headBox.CanCollide = false
    headBox.CanTouch = true
    headBox.Massless = true
    headBox.Anchored = false
    headBox.CFrame = head.CFrame
    headBox.Parent = zombie

    local weldHead = Instance.new("WeldConstraint")
    weldHead.Part0 = head
    weldHead.Part1 = headBox
    weldHead.Parent = headBox

    L.zombieHitboxAddedParts[zombie] = { outer = outer, head = headBox }
end

local function removeHitboxesFromZombie(zombie)
    local parts = L.zombieHitboxAddedParts[zombie]
    if parts then
        if parts.outer then parts.outer:Destroy() end
        if parts.head then parts.head:Destroy() end
        L.zombieHitboxAddedParts[zombie] = nil
    else
        if zombie and zombie.Parent then
            for _, child in zombie:GetChildren() do
                if child.Name == "ZombieHitbox_Outer" or child.Name == "ZombieHitbox_Head" then
                    child:Destroy()
                end
            end
        end
    end
end

local function refreshZombieHitboxes()
    if not L.zombieHitboxEnabled then
        local toRemove = {}
        for zombie, _ in L.zombieHitboxAddedParts do
            table.insert(toRemove, zombie)
        end
        for _, zombie in toRemove do
            removeHitboxesFromZombie(zombie)
        end
        L.zombieHitboxAddedParts = {}
        return
    end

    local allZombies = {}
    local function scanFolder(folder)
        if not folder then return end
        for _, z in folder:GetChildren() do
            if z:IsA("Model") and (z.Name == "m_Zombie" or z:FindFirstChild("Orig")) then
                if not isZombieDead(z) then
                    table.insert(allZombies, z)
                end
            end
        end
    end
    scanFolder(workspace:FindFirstChild("Zombies"))
    scanFolder(workspace:FindFirstChild("Camera"))

    local toRemove = {}
    for zombie, _ in L.zombieHitboxAddedParts do
        local stillExists = false
        if zombie.Parent and not isZombieDead(zombie) then
            for _, z in allZombies do
                if z == zombie then stillExists = true; break end
            end
        end
        if not stillExists then
            table.insert(toRemove, zombie)
        end
    end
    for _, zombie in toRemove do
        removeHitboxesFromZombie(zombie)
    end

    for _, z in allZombies do
        if not L.zombieHitboxAddedParts[z] then
            addHitboxesToZombie(z)
        end
    end
end

local function updateAllZombieHitboxVisuals()
    local currentTrans = getCurrentTransparency()
    for _, parts in L.zombieHitboxAddedParts do
        if parts.outer and parts.outer.Parent then
            parts.outer.Size = Vector3.new(L.zombieHitboxSize, L.zombieHitboxSize, L.zombieHitboxSize)
            parts.outer.Transparency = currentTrans
        end
        if parts.head and parts.head.Parent then
            parts.head.Size = Vector3.new(L.zombieHitboxSize/2, L.zombieHitboxSize/2, L.zombieHitboxSize/2)
            parts.head.Transparency = currentTrans
        end
    end
end

local function onZombieAdded(zombie)
    if L.zombieHitboxEnabled and zombie:IsA("Model") and (zombie.Name == "m_Zombie" or zombie:FindFirstChild("Orig")) then
        task.wait(0.1)
        if not isZombieDead(zombie) then
            addHitboxesToZombie(zombie)
        end
    end
end

local zombiesFolder = workspace:FindFirstChild("Zombies")
if zombiesFolder then
    zombiesFolder.ChildAdded:Connect(onZombieAdded)
end
local cameraFolder = workspace:FindFirstChild("Camera")
if cameraFolder then
    cameraFolder.ChildAdded:Connect(onZombieAdded)
end

local function ensureHighlight(target, fillColor, outlineColor)
    if not target or not target.Parent or isZombieDead(target) then return end
    local h = target:FindFirstChild("ESP_Highlight")
    if not h then
        h = Instance.new("Highlight")
        h.Name = "ESP_Highlight"
        h.Parent = target
        h.OutlineColor = outlineColor or Color3.fromRGB(255,255,255)
    end
    h.FillColor = fillColor or Color3.fromRGB(255,255,255)
end

local function removeHighlight(target)
    if not target then return end
    local h = target:FindFirstChild("ESP_Highlight")
    if h then h:Destroy() end
end

local function scanAndApply()
    pcall(function()
        local function processFolder(folder)
            if not folder then return end
            for _, child in pairs(folder:GetChildren()) do
                if child:IsA("Model") and (child.Name == "m_Zombie" or child:FindFirstChild("Orig") or child:FindFirstChild("Humanoid")) then
                    if isZombieDead(child) then
                        removeHighlight(child)
                        removeHitboxesFromZombie(child)
                    else
                        local types = getZombieClassification(child)
                        local highlighted = false

                        if espShamblerToggled and types.shambler then
                            ensureHighlight(child, Color3.fromRGB(200, 200, 200))
                            highlighted = true
                        elseif espRToggled and types.runner then
                            ensureHighlight(child, Color3.fromRGB(255, 0, 0))
                            highlighted = true
                        elseif espBToggled and types.bomber then
                            ensureHighlight(child, Color3.fromRGB(65, 105, 225))
                            highlighted = true
                        elseif espCuToggled and types.cuirassier then
                            ensureHighlight(child, Color3.fromRGB(65, 105, 225))
                            highlighted = true
                        elseif espZToggled and types.zapper then
                            ensureHighlight(child, Color3.fromRGB(255, 0, 255))
                            highlighted = true
                        elseif espIgniterToggled and types.igniter then
                            ensureHighlight(child, Color3.fromRGB(255, 69, 0))
                            highlighted = true
                        end

                        if not highlighted then
                            removeHighlight(child)
                        end
                    end
                end
            end
        end

        processFolder(Workspace.Camera)
        processFolder(Workspace:FindFirstChild("Zombies"))
    end)
end

RunService.Heartbeat:Connect(scanAndApply)

task.spawn(function()
    while true do
        task.wait(2)
        if L.zombieHitboxEnabled then
            refreshZombieHitboxes()
        end
        if L.removeParticlesEnabled then
            for _, desc in pairs(workspace:GetDescendants()) do
                if desc:IsA("ParticleEmitter") or desc:IsA("Fire") or desc:IsA("Smoke") or desc:IsA("Sparkles") then desc:Destroy() end
            end
        end
        if L.removeHorsesEnabled then
            for _, obj in pairs(workspace:GetDescendants()) do
                if obj:IsA("Model") or obj:IsA("BasePart") then
                    local name = obj.Name:lower()
                    if name:find("horse") or name:find("cavalry") or name:find("pony") then obj:Destroy() end
                end
            end
        end
    end
end)

local Window = Library:CreateWindow({
    Title = "Tomboy Hub",
    Footer = "Obsidian UI",
    Icon = 0000000000,
    NotifySide = "Right",
    ShowCustomCursor = false,
})

local Tabs = {
    Combat = Window:AddTab("Combat", "swords"),
    Aimbot = Window:AddTab("Aimbot", "crosshair"),
    ESP = Window:AddTab("ESP", "eye"),
    Antilag = Window:AddTab("Antilag", "zap"),
    ['UI Settings'] = Window:AddTab("UI Settings", "settings"),
}

local CombatBox = Tabs.Combat:AddLeftGroupbox("Combat Enhancements")
local HitboxBox = Tabs.Combat:AddRightGroupbox("Zombie Hitboxes")

local AimbotMainBox = Tabs.Aimbot:AddLeftGroupbox("Aimbot General Settings")
local TargetFilterBox = Tabs.Aimbot:AddRightGroupbox("Aimbot Target Filters")

local ESPBox = Tabs.ESP:AddLeftGroupbox("Zombie ESP Highlights")
local AntilagBox = Tabs.Antilag:AddLeftGroupbox("Performance Optimizations")

CombatBox:AddToggle('Tomboyhitter', {
    Text = 'Tomboyhitter',
    Default = false,
    Tooltip = 'its in the name you dummy',
    Callback = function(Value) L.tomboyHitterEnabled = Value end
})

CombatBox:AddToggle('TomboyMusician', {
    Text = 'tomboy musician',
    Default = false,
    Tooltip = 'Automatically plays instrument notes perfectly on beat.',
    Callback = function(Value) _G.MusicianAutoPlay = Value end
})

CombatBox:AddToggle('SpeedToggle', {
    Text = 'Tomboy Speed',
    Default = false,
    Tooltip = 'Attack speed adjust.',
    Callback = function(Value)
        L.attackSpeedEnabled = Value
        L.updateAllTools()
    end
})

CombatBox:AddSlider('SpeedSlider', {
    Text = 'Tomboy Speed Multiplier',
    Default = 1.0, Min = 0.5, Max = 10.0, Suffix = 'x', Rounding = 1,
    Callback = function(Value)
        L.attackSpeedMultiplier = Value
        if L.attackSpeedEnabled then L.updateAllTools() end
    end
})

AimbotMainBox:AddToggle('AimbotToggle', {
    Text = 'Enable AIMBOT',
    Default = false,
    Callback = function(Value) L.aimbotEnabled = Value end
})

AimbotMainBox:AddDropdown('AimbotTargetPart', {
    Text = 'Aim Target Part',
    Values = {'Head', 'Body'},
    Default = 1,
    Callback = function(Value) L.aimbotTargetPart = Value end
})

AimbotMainBox:AddSlider('AimbotMaxRange', {
    Text = 'Max Weapon Range',
    Default = 500, Min = 50, Max = 2000, Suffix = ' studs', Rounding = 0,
    Callback = function(Value) L.aimbotMaxRange = Value end
})

AimbotMainBox:AddToggle('FOVCircleToggle', {
    Text = 'Show FOV Circle',
    Default = false,
    Callback = function(Value) L.fovCircleVisible = Value end
})

AimbotMainBox:AddSlider('AimbotFOV', {
    Text = 'Target Range FOV Size',
    Default = 300, Min = 50, Max = 600, Suffix = ' px', Rounding = 0,
    Callback = function(Value) L.aimbotFOV = Value end
})

TargetFilterBox:AddToggle('TargetShambler', { Text = 'aim Shambler', Default = true, Callback = function(Value) L.targetShambler = Value end })
TargetFilterBox:AddToggle('TargetRunner', { Text = 'aim Runner', Default = true, Callback = function(Value) L.targetRunner = Value end })
TargetFilterBox:AddToggle('TargetBomber', { Text = 'aim Bomber', Default = true, Callback = function(Value) L.targetBomber = Value end })
TargetFilterBox:AddToggle('TargetCuirassier', { Text = 'aim Cuirassier', Default = true, Callback = function(Value) L.targetCuirassier = Value end })
TargetFilterBox:AddToggle('TargetZapper', { Text = 'aim Zapper', Default = true, Callback = function(Value) L.targetZapper = Value end })
TargetFilterBox:AddToggle('TargetIgniter', { Text = 'aim Igniter', Default = true, Callback = function(Value) L.targetIgniter = Value end })

HitboxBox:AddToggle('HitboxToggle', {
    Text = 'Enable Hitbox Expander',
    Default = false,
    Callback = function(Value)
        L.zombieHitboxEnabled = Value
        refreshZombieHitboxes()
    end
})

HitboxBox:AddToggle('HitboxVisualsToggle', {
    Text = 'Enable Hitbox Visuals',
    Default = true,
    Callback = function(Value)
        L.zombieHitboxVisualsEnabled = Value
        updateAllZombieHitboxVisuals()
    end
})

HitboxBox:AddSlider('HitboxSizeSlider', {
    Text = 'Hitbox Size',
    Default = 10, Min = 2, Max = 30, Suffix = ' studs', Rounding = 0,
    Callback = function(Value)
        L.zombieHitboxSize = Value
        updateAllZombieHitboxVisuals()
    end
})

HitboxBox:AddSlider('HitboxTransSlider', {
    Text = 'Hitbox Transparency',
    Default = 50, Min = 0, Max = 100, Suffix = '%', Rounding = 0,
    Callback = function(Value)
        L.zombieHitboxTransparencyPercent = Value
        updateAllZombieHitboxVisuals()
    end
})

ESPBox:AddToggle('ESPShamblerToggle', { Text = 'ESP Shambler', Default = false, Callback = function(Value) espShamblerToggled = Value end })
ESPBox:AddToggle('ESPRunnerToggle', { Text = 'ESP Runner (Fast)', Default = false, Callback = function(Value) espRToggled = Value end })
ESPBox:AddToggle('ESPBomberToggle', { Text = 'ESP Bomber', Default = false, Callback = function(Value) espBToggled = Value end })
ESPBox:AddToggle('ESPCuirassToggle', { Text = 'ESP Cuirassier', Default = false, Callback = function(Value) espCuToggled = Value end })
ESPBox:AddToggle('ESPZapperToggle', { Text = 'ESP Zapper', Default = false, Callback = function(Value) espZToggled = Value end })
ESPBox:AddToggle('ESPIgniterToggle', { Text = 'ESP Igniter', Default = false, Callback = function(Value) espIgniterToggled = Value end })

AntilagBox:AddToggle('RemoveParticlesTest', {
    Text = 'Removes Particles',
    Default = false,
    Tooltip = 'Deletes particle emitters, fire, and smoke effects for FPS boost.',
    Callback = function(Value) L.removeParticlesEnabled = Value end
})

AntilagBox:AddToggle('RemoveHorsesToggle', {
    Text = 'Removes Horses (Westminster/Catacombs)',
    Default = false,
    Tooltip = 'Cleans up horse models and related map heavy entities.',
    Callback = function(Value) L.removeHorsesEnabled = Value end
})

local MenuGroup = Tabs['UI Settings']:AddLeftGroupbox('Menu Settings')
MenuGroup:AddButton('destroy thou script', function() 
    for zombie, _ in L.zombieHitboxAddedParts do
        removeHitboxesFromZombie(zombie)
    end
    fovDrawingCircle:Remove()
    lockBox:Remove()
    Library:Unload() 
end)

if successTM and ThemeManager then
    ThemeManager:SetLibrary(Library)
    ThemeManager:SetFolder("TomboyHubConfigs")
    ThemeManager:ApplyToTab(Tabs['UI Settings'])
end

if successSM and SaveManager then
    SaveManager:SetLibrary(Library)
    SaveManager:IgnoreThemeSettings()
    SaveManager:SetFolder("TomboyHubConfigs")
    SaveManager:BuildConfigSection(Tabs['UI Settings'])
    SaveManager:LoadAutoloadConfig()
end

print("tomboy hub loaded successfully with tomboys, musician, and no flinging bugs!")
